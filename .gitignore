# Local vendor clone for syncing upstream agent-skills (not committed)
.vendor/

node_modules
.venv/
.qwen/settings.json.orig
!install.sh
install*.sh*
!install.sh

# skillgrid-cli build artifacts (Taskfile at repo root builds into dist/)
/dist/
skillgrid-cli/dist/
skillgrid-cli/skillgrid
skillgrid-cli/bin/
skillgrid-cli/out/

# Generated skill registry cache (regenerated by skill-registry refresh)
.atl/
.gitworktree/
# .skillgrid: specs/ (active) + archive/ (closed) are committed; only scratch is ignored.
.skillgrid/sdd/
.skillgrid/.cleave/
.skillgrid/brainstorm/

###############################
# CocoIndex Code (ccc)
###############################
# done
.codegraph/
# done (ollama)
graphify-out/
# done (ollama)
/.cocoindex_code/
# done
*gitnexus*
###############################

# skillgrid-ui: track the source tree, ignore only build/deps artifacts
skillgrid-ui/node_modules/
skillgrid-ui/dist/

